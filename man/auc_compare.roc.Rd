% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/012-roc.R
\name{auc_compare.roc}
\alias{auc_compare.roc}
\title{Compare two paired ROC AUCs}
\usage{
\method{auc_compare}{roc}(
  x,
  curves,
  method = c("ep24", "delong"),
  conf.level = 0.95,
  rating_method = c("pearson", "kendall"),
  name = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{roc} object.}

\item{curves}{Character vector naming exactly two marker or MLR curves. The
reported difference is \code{curves[1] - curves[2]}.}

\item{method}{Paired comparison method: \code{"ep24"} (default) or \code{"delong"}.}

\item{conf.level}{Confidence level for the normal-approximation interval.}

\item{rating_method}{Correlation method for EP24 paired scores: \code{"pearson"}
for interval-scale laboratory results (default) or \code{"kendall"} for
ordinal ratings. Ignored for DeLong.}

\item{name}{Optional unique name for storing the comparison. By default it
is generated from the two curve names and method.}

\item{...}{Additional arguments (currently unused).}
}
\value{
The updated \code{roc} object, invisibly. The comparison is stored in
\code{x$auc_comparisons[[name]]} as a \code{roc_auc_comparison} object.
}
\description{
Compare two ROC curves evaluated on the common complete cases stored in a
single \code{\link[=roc]{roc()}} object. A curve can be a marker previously processed by
\code{\link[=auc]{auc()}} or a named multivariate model produced by \code{\link[=mlr]{mlr()}}.
}
\details{
\code{method = "ep24"} implements the paired Hanley--McNeil approximation in
CLSI EP24-A2. It combines the two single-curve Hanley--McNeil standard
errors with the correlation between the paired AUCs. The latter is obtained
by linear interpolation of EP24-A2 Table 5 from the average within-group
score correlation and average AUC. Coordinates outside the published table
are bounded to its range and recorded in the result.

\code{method = "delong"} uses the nonparametric DeLong covariance estimator and
handles ties with half credit. Both methods test the two-sided null
hypothesis that the AUC difference is zero.
}
\examples{
obj <- roc(ivd_roc_example, c("x1", "x2"), "ref")
obj <- auc(obj)
obj <- auc_compare(obj, c("x1", "x2"), method = "ep24")
obj$auc_comparisons[[1L]]

obj <- mlr(obj, name = "combined")
obj <- auc_compare(obj, c("x1", "combined"), method = "delong")
}
