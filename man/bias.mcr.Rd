% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/007-method-comparison.R
\name{bias.mcr}
\alias{bias.mcr}
\title{Bias analysis (S3 method) -- estimate bias at given medical decision levels}
\usage{
\method{bias}{mcr}(
  x,
  mdl,
  level = 0.95,
  interval = c("", "confidence", "prediction", "both"),
  ci_method = c("auto", "analytic", "jackknife", "rank", "bootstrap_percentile",
    "bootstrap_standard"),
  bootstrap_replicates = 5000L,
  seed = NULL,
  ...
)
}
\arguments{
\item{x}{mcr object (must call regression() first)}

\item{mdl}{Medical decision levels (numeric vector) on the reference/X scale}

\item{level}{Confidence level, default 0.95}

\item{interval}{Interval type: "" (point estimate, default), "confidence" (confidence interval),
"prediction" (prediction interval), "both" (confidence + prediction)}

\item{ci_method}{Parameter-uncertainty method. \code{"auto"} uses percentile
Bootstrap for Passing-Bablok and otherwise reuses the regression method.}

\item{bootstrap_replicates}{Number of paired Bootstrap resamples; at least
5000 when a Bootstrap method is used.}

\item{seed}{Optional integer seed.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Updated mcr object (invisible), results stored in $bias
}
\description{
Compute bias at specified medical decision levels (MDL) based on stored regression results:
bias = predicted_candidate - reference
= (intercept + slope * x0) - x0
= intercept + (slope - 1) * x0
}
\details{
Bias follows the conventional candidate-minus-reference direction; a
positive value indicates that the candidate method is higher.
}
\examples{
  obj <- mcr(ivd_mcr_example, "sid", "test", "ref")
  obj <- regression(obj, method = "ols")
  bias(obj, mdl = c(200, 400, 600))
  bias(obj, mdl = c(200, 400, 600), interval = "both")
}
