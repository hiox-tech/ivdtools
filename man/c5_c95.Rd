% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/018-c5-c95.R
\name{c5_c95}
\alias{c5_c95}
\title{Estimate C5 and C95 from an internal continuous response}
\usage{
c5_c95(
  data,
  result = NULL,
  sample = NULL,
  cutoff,
  direction = c("geq", "leq"),
  probabilities = c(0.05, 0.95),
  component = NULL,
  model.no = 1:10,
  K = 2,
  search_range = NULL,
  tol = 1e-07
)
}
\arguments{
\item{data}{A data frame of replicate continuous responses, or a
\code{precision} object containing a fitted precision profile.}

\item{result}{Name of the numeric response column. Required for a data frame.}

\item{sample}{Name of the sample or level column. Required for a data frame.}

\item{cutoff}{One or more finite decision cutoffs.}

\item{direction}{Direction defining the condition of interest: \code{"geq"}
means response greater than or equal to the cutoff; \code{"leq"} means
response less than or equal to the cutoff.}

\item{probabilities}{Probabilities to estimate, default \code{c(0.05, 0.95)}.}

\item{component}{Precision-profile component used when \code{data} is a
\code{precision} object. Defaults to \code{"total"} when available.}

\item{model.no}{Candidate VFP model numbers for raw data.}

\item{K}{Fixed exponent supplied to applicable VFP models.}

\item{search_range}{Optional two-element finite numeric range for root
finding. By default, the observed mean range is expanded automatically.}

\item{tol}{Positive numeric root-finding tolerance.}
}
\value{
An object of class \code{c5_c95}. Its \code{estimates} element contains
the estimated response, fitted SD, achieved probability, extrapolation flag,
and root-search diagnostics for each cutoff and probability.
}
\description{
Estimates response values associated with selected probabilities of a
qualitative result. The response SD is modeled as a function of the expected
response using a VFP precision profile. For \code{direction = "geq"}, the event
is \code{result >= cutoff}; for \code{direction = "leq"}, the event is
\code{result <= cutoff}. No acceptance decision is made.
}
\examples{
set.seed(1203)
cx_data <- data.frame(
  level = rep(paste0("L", 1:7), each = 20),
  response = unlist(lapply(seq(34, 46, length.out = 7), function(mu)
    stats::rnorm(20, mean = mu, sd = 0.7 + 0.01 * mu)))
)
cx <- c5_c95(cx_data, result = "response", sample = "level",
             cutoff = 40, model.no = 1)
cx$estimates
plot(cx)
}
