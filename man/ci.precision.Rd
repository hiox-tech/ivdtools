% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/008-precision.R
\name{ci.precision}
\alias{ci.precision}
\title{S3 confidence interval method}
\usage{
\method{ci}{precision}(x, digits = 4, ...)
}
\arguments{
\item{x}{\code{precision} object}

\item{digits}{Number of decimal places, default \code{4}}

\item{...}{Reserved arguments}
}
\value{
Updated \code{precision} object with results stored in \code{$ci}
}
\description{
Compute confidence intervals for each variance component (SD and \%CV) based on
Satterthwaite approximation. Requires \code{variance()} to be run first to populate
the \code{$results} slot.
}
\details{
This method reports intervals for the fitted model components and
the total component. EP05 semantic combinations such as multisite
within-laboratory precision require the \code{day}, \code{run}, and \code{site} mapping
supplied to \code{\link{report.precision}} and are therefore constructed
by \code{report(x, ..., table = "CI")}.
}
\examples{
  data(VCAdata1, package = "VCA")
  obj <- precision(VCAdata1, y ~ day/run, by = "sample")
  obj <- variance(obj)
  obj <- ci(obj)
}
