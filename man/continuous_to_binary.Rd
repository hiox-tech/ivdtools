% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/012-roc.R
\name{continuous_to_binary}
\alias{continuous_to_binary}
\title{Convert quantitative columns to binary based on cutoff values}
\usage{
continuous_to_binary(data, cols, cutoff)
}
\arguments{
\item{data}{data frame}

\item{cols}{character vector of column names to binarize}

\item{cutoff}{numeric vector of cutoff values; recycled to \code{length(cols)}
if a single value is given}
}
\value{
data frame with original columns plus new \code{<col>_binary} columns
}
\description{
Creates new binary columns (0/1) from existing quantitative columns using
specified cutoff values, appending them to the original data frame.
New columns are named \code{<col>_binary}.
}
\examples{
  df <- data.frame(x = 1:10, y = rnorm(10))
  continuous_to_binary(df, "x", 5)
  continuous_to_binary(df, c("x", "y"), c(5, 0))
}
