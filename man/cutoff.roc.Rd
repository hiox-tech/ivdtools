% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/012-roc.R
\name{cutoff.roc}
\alias{cutoff.roc}
\title{Optimal Cutoff Analysis (S3 method)}
\usage{
\method{cutoff}{roc}(x, cols = NULL, ...)
}
\arguments{
\item{x}{roc object}

\item{cols}{column names to analyze; default is all}

\item{...}{reserved arguments}
}
\value{
Updated roc object with results stored in $cutoff_table
}
\description{
For each evaluation column, compute diagnostic metrics at all cutoff values,
and identify the optimal cutoff (maximum Youden index, closest to (0,1) corner).
}
\examples{
  obj <- roc(ivd_roc_example, cols = c("x1", "x2"), reference = "ref")
  obj <- auc(obj)
  cutoff(obj)
  cutoff(obj, cols = "x1")
}
