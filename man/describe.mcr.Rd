% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/007-method-comparison.R
\name{describe.mcr}
\alias{describe.mcr}
\title{Compute descriptive statistics and store results}
\usage{
\method{describe}{mcr}(x, cols = NULL, digits = 4L, ...)
}
\arguments{
\item{x}{mcr object}

\item{cols}{Column name vector to analyze; by default, analyzes all columns except
id, candidate, and reference. Supports exclusion with \code{-} prefix,
e.g., cols = -c("age", "sex").}

\item{digits}{Number of decimal places, default 4}

\item{...}{Additional arguments}
}
\value{
Updated \code{mcr} object (invisible), results stored in \code{x$describe}
}
\description{
Computes data overview (rows/columns/ID duplicates), per-column summaries,
and a side-by-side comparison table of candidate, reference, and Diff.
Results are stored in \code{x$describe} (class \code{mcr_describe}) and
displayed via \code{print.mcr_describe()} (e.g., when calling \code{summary()}).
}
\examples{
  obj <- mcr(ivd_mcr_example, "sid", "test", "ref")
  describe(obj)
}
