% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/019-dilution.R
\name{hook_effect}
\alias{hook_effect}
\title{Summarize a high-dose hook-effect experiment}
\usage{
hook_effect(
  data,
  concentration,
  response,
  series = NULL,
  uloq,
  uloq_response = NULL,
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame containing known concentrations and raw responses.}

\item{concentration}{Numeric known-concentration column.}

\item{response}{Numeric raw-response column.}

\item{series}{Optional column identifying independent hook experiments.}

\item{uloq}{Finite concentration representing the upper limit of quantitation.}

\item{uloq_response}{Optional raw response at the ULoQ. It may be one finite
value for all series or one value per series. When omitted, each series
must contain measurements at \code{concentration == uloq}.}

\item{conf.level}{Confidence level for mean raw response at each concentration.}
}
\value{
An object of class \code{hook_effect} containing concentration-level
summaries and one threshold result per series.
}
\description{
Summarizes raw response by known concentration and identifies the first
post-peak measured concentration whose mean response is at or below the raw
response associated with the ULoQ. The reported \code{hook_concentration} is the
immediately preceding measured concentration; no interpolation is used. No
hook-effect or extended-interval acceptability decision is made.
}
\examples{
hook_data <- data.frame(
  concentration = rep(c(100, 200, 400, 800), each = 2),
  response = c(48, 52, 98, 102, 148, 152, 88, 92)
)
hook <- hook_effect(
  hook_data, "concentration", "response", uloq = 200
)
hook$results
}
\references{
CLSI. Establishing and Verifying an Extended Measuring Interval
Through Specimen Dilution and Spiking. EP34, 1st ed. 2018.
}
