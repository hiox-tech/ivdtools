% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/017-interference.R
\name{interference_paired}
\alias{interference_paired}
\title{Analyze paired-difference interference data}
\usage{
interference_paired(
  data,
  result,
  design = c("ep07", "analyte_spike", "blank"),
  by = NULL,
  condition = NULL,
  test_level = "test",
  control_level = "control",
  matrix = NULL,
  normal_level = "normal",
  interferent_level = "interferent",
  spike = NULL,
  unspiked_level = "unspiked",
  spiked_level = "spiked",
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame in long format.}

\item{result}{Numeric measurement-result column.}

\item{design}{\code{"ep07"}, \code{"analyte_spike"}, or \code{"blank"}.}

\item{by}{Optional character vector of stratification columns.}

\item{condition}{Test/control condition column for \code{ep07}, or optional
condition column for \code{blank}.}

\item{test_level}{Value identifying the test/interferent condition.}

\item{control_level}{Value identifying the solvent control condition.}

\item{matrix}{Matrix-type column for \code{analyte_spike}.}

\item{normal_level}{Value identifying the normal matrix.}

\item{interferent_level}{Value identifying the interferent-containing matrix.}

\item{spike}{Spike-status column for \code{analyte_spike}.}

\item{unspiked_level}{Value identifying measurements before analyte spiking.}

\item{spiked_level}{Value identifying measurements after analyte spiking.}

\item{conf.level}{Confidence level for intervals.}
}
\value{
An S3 object of class \code{interference_paired} containing \code{effects},
\code{cell_summary}, \code{issues}, and excluded row indices.
}
\description{
Supports the standard EP07 test/control preparation and two laboratory
extensions: analyte spiking in normal/interferent matrices and direct
measurement of interferent dissolved in a blank. One row represents one
replicate result. Multiple interferents or measurand levels are handled by
supplying their columns in \code{by}.
}
\examples{
ep07_data <- data.frame(
  interferent = rep(c("bilirubin", "hemoglobin"), each = 10),
  condition = rep(rep(c("control", "test"), each = 5), 2),
  result = c(0.45, 0.56, 0.48, 0.54, 0.47,
             0.24, 0.28, 0.35, 0.37, 0.26,
             5.02, 4.98, 5.08, 4.95, 5.00,
             5.20, 5.12, 5.18, 5.10, 5.15)
)
interference_paired(ep07_data, "result", design = "ep07",
                    condition = "condition", by = "interferent")

spike_data <- expand.grid(
  matrix = c("normal", "interferent"), spike = c("before", "after"),
  replicate = 1:4, KEEP.OUT.ATTRS = FALSE
)
spike_data$result <- with(spike_data,
  ifelse(matrix == "normal", 10, 12) +
  ifelse(spike == "after", ifelse(matrix == "normal", 10, 8), 0))
interference_paired(spike_data, "result", design = "analyte_spike",
                    matrix = "matrix", spike = "spike",
                    unspiked_level = "before", spiked_level = "after")
}
