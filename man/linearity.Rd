% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/016-linearity.R
\name{linearity}
\alias{linearity}
\title{Evaluate linearity using straight-line or polynomial regression}
\usage{
linearity(
  data,
  sample,
  result,
  x,
  method = c("linear", "polynomial"),
  intercept = TRUE,
  weights = c("equal", "level_variance", "pooled", "precision_profile"),
  variance_group = NULL,
  max_degree = 3L,
  poly_alpha = 0.05,
  percent_base = c("predicted", "x", "mean"),
  conf.level = NULL,
  multiplicity = c("familywise", "none"),
  adl = NULL,
  adl_abs = NULL,
  adl_rel = NULL,
  profile_exclude_low = TRUE,
  profile_intercept = FALSE
)
}
\arguments{
\item{data}{Data frame in long format, with one row per replicate result.}

\item{sample}{Sample-level identifier column name.}

\item{result}{Numeric measurement-result column name.}

\item{x}{Numeric expected value, assigned value, relative concentration, or
HIGH-proportion column name. Its value must be constant within a sample.}

\item{method}{\code{"linear"} or \code{"polynomial"}.}

\item{intercept}{Include an intercept in every fitted model?}

\item{weights}{One of \code{"equal"}, \code{"level_variance"}, \code{"pooled"}, or
\code{"precision_profile"}; alternatively, a numeric vector with one weight per
observation or one weight per sample level (in increasing x order).}

\item{variance_group}{When \code{weights = "pooled"}, a single character string
naming the column that assigns every sample level to an explicitly chosen
contiguous variance group. The value must be constant within a sample,
and every group must contain at least two sample levels. It must be \code{NULL}
for other weighting methods.}

\item{max_degree}{Highest polynomial degree, 2 or 3.}

\item{poly_alpha}{Significance level for EP6-A nonlinear coefficients.}

\item{percent_base}{Denominator for percentage residuals and polynomial
deviation from linearity: the straight-line prediction, x value, or
observed sample mean. EP06-Ed2 examples use the prediction.}

\item{conf.level}{Optional overall or pointwise confidence level for
straight-line residuals. Use \code{NULL} to omit confidence intervals.}

\item{multiplicity}{\code{"familywise"} adjusts intervals so their joint
coverage is \code{conf.level}; \code{"none"} applies \code{conf.level} to each level.}

\item{adl}{Optional direct absolute ADL, scalar or one value per level.}

\item{adl_abs}{Optional fixed absolute ADL.}

\item{adl_rel}{Optional relative ADL in percent. If more than one ADL form
is supplied, the largest allowable value is used at each level.}

\item{profile_exclude_low}{Exclude the lowest-x level from the precision
profile and use its observed SD directly?}

\item{profile_intercept}{Include an intercept in the SD-on-mean precision
profile? EP06 uses a zero intercept.}
}
\value{
An S3 object of class \code{linearity}.
}
\description{
\code{linearity()} is a parameter-driven analysis engine rather than a separate
validation/verification workflow. The straight-line calculations implement
the approach in CLSI EP06-Ed2. Polynomial regression reproduces the
superseded EP6-A procedure and is reported as exploratory. Straight-line
models are fitted to sample-level means; EP6-A polynomial models are fitted
to the retained replicate observations.
}
\details{
With \code{weights = "pooled"}, variance groups must be supplied explicitly.
Within group \code{g}, the pooled variance is
\code{sum((n[j] - 1) * s[j]^2) / sum(n[j] - 1)}, and the level-mean regression
weight is \code{n[j] / pooled_variance[g]}. The function does not infer groups
from the number of concentration levels because EP06 grouping also depends
on the observed repeatability pattern.
}
\examples{
set.seed(2020)
linearity_data <- data.frame(
  level = rep(1:6, each = 4),
  expected = rep(c(10, 30, 50, 70, 90, 110), each = 4)
)
linearity_data$result <- 2 + 0.98 * linearity_data$expected +
  stats::rnorm(nrow(linearity_data), sd = 1.5)

# Parameter-driven straight-line analysis.
fit_linear <- linearity(
  linearity_data, sample = "level", result = "result", x = "expected",
  method = "linear", intercept = TRUE, weights = "level_variance",
  adl_rel = 5
)
fit_linear$model_comparison
fit_linear$level_summary

# EP6-A polynomial analysis (reported as exploratory under EP06-Ed2).
fit_polynomial <- linearity(
  linearity_data, sample = "level", result = "result", x = "expected",
  method = "polynomial", max_degree = 3
)
fit_polynomial$coefficients
}
