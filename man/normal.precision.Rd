% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/008-precision.R
\name{normal.precision}
\alias{normal.precision}
\title{S3 normality test method}
\usage{
\method{normal}{precision}(
  x,
  method = c("auto", "shapiro", "sw", "ad", "lillie", "cvm"),
  level = 0.95,
  ...
)
}
\arguments{
\item{x}{\code{precision} object}

\item{method}{Test method: \code{"auto"} (default, automatic selection), \code{"shapiro"},
\code{"sw"}, \code{"ad"}, \code{"lillie"}, \code{"cvm"}}

\item{level}{Confidence level, default \code{0.95}}

\item{...}{Reserved arguments}
}
\value{
Updated \code{precision} object with results stored in \code{$normal}.
Each sample entry contains \code{test_method}, \code{statistic_name},
\code{statistic}, \code{p_value}, and \code{n}; \code{W} is retained as a
legacy alias only for Shapiro-Wilk results.
}
\description{
Perform normality tests on the response values for each sample, underlying call to \code{normal_test()}.
Supports automatic test selection (Shapiro-Wilk, Anderson-Darling, Lilliefors, CVM).
}
\examples{
  data(VCAdata1, package = "VCA")
  obj <- precision(VCAdata1, y ~ day/run, by = "sample")
  obj <- normal(obj)
}
