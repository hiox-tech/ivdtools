% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/007-method-comparison.R
\name{outlier.mcr}
\alias{outlier.mcr}
\title{Outlier detection (S3 method)}
\usage{
\method{outlier}{mcr}(
  x,
  method = c("grubbs", "esd", "dixon", "iqr"),
  type = c("difference", "percent"),
  percent_denominator = c("reference", "mean", "candidate"),
  alpha = 0.05,
  coef = 1.5,
  ...
)
}
\arguments{
\item{x}{mcr object}

\item{method}{Outlier detection method: "grubbs" (default), "esd", "dixon", "iqr"}

\item{type}{Data type to compute: "difference" (default) or "percent" (percent difference)}

\item{percent_denominator}{Denominator for percent differences: reference
(default), pair mean, or candidate.}

\item{alpha}{Significance level, default 0.05 (Grubbs/ESD/Dixon only)}

\item{coef}{IQR multiplier, default 1.5 (IQR only)}

\item{...}{Additional arguments passed to esd_test / dixon_test}
}
\value{
Updated mcr object (invisible), results stored in $outlier
}
\description{
Perform outlier detection based on the difference or percent difference between two measurements.
Reuses four methods from ../precision/outliers.R.
}
\examples{
  obj <- mcr(ivd_mcr_example, "sid", "test", "ref")
  outlier(obj, method = "grubbs")
  outlier(obj, method = "iqr", type = "percent")
}
