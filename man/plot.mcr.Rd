% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/007-method-comparison.R
\name{plot.mcr}
\alias{plot.mcr}
\title{Plot (S3 method) -- unified plotting interface}
\usage{
\method{plot}{mcr}(
  x,
  type = c("scatter", "regression", "bland_altman", "bias"),
  interval = c("none", "confidence", "prediction", "both"),
  conf.level = NULL,
  mdl = NULL,
  ...
)
}
\arguments{
\item{x}{mcr object}

\item{type}{Plot type: "scatter" (scatter + identity line, default),
"regression" (regression line + confidence/prediction bands),
"bland_altman" (Bland-Altman plot),
"bias" (bias trend plot)}

\item{interval}{Interval type (only for type="regression"): "none" (default),
"confidence", "prediction", "both"}

\item{conf.level}{Confidence level; if NULL, reads from analysis slot, otherwise uses this value}

\item{mdl}{Medical decision levels (only for type="bias"); if NULL, tries to read
from x$bias$mdl}

\item{...}{Additional arguments}
}
\value{
Invisible ggplot object
}
\description{
Draw one of four plot types for an mcr object.
}
\examples{
  obj <- mcr(ivd_mcr_example, "sid", "test", "ref")
  plot(obj)
  plot(obj, type = "scatter")
  obj <- regression(obj)
  plot(obj, type = "regression", interval = "confidence")
  obj <- bland_altman(obj)
  plot(obj, type = "bland_altman")
  obj <- bias(obj, mdl = c(200, 400))
  plot(obj, type = "bias")
}
