% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/020-uncertainty.R
\name{plot.uncertainty_propagation}
\alias{plot.uncertainty_propagation}
\title{Plot propagated uncertainty}
\usage{
\method{plot}{uncertainty_propagation}(x, type = c("distribution", "contribution"), ...)
}
\arguments{
\item{x}{An \code{uncertainty_propagation} object.}

\item{type}{\code{"distribution"} or \code{"contribution"}.}

\item{...}{Reserved arguments.}
}
\value{
A ggplot object, invisibly.
}
\description{
Plot propagated uncertainty
}
\examples{
numerator <- uncertainty_type_b(
  "numerator", estimate = 10, uncertainty = 0.2, k = 2,
  distribution = "normal"
)
denominator <- uncertainty_type_b(
  "denominator", estimate = 2, uncertainty = 0.04, k = 2,
  distribution = "normal"
)
propagated <- uncertainty_propagate(
  function(numerator, denominator) numerator / denominator,
  list(numerator, denominator), method = "delta"
)
plot(propagated, type = "contribution")
}
