% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/008-precision.R
\name{precision}
\alias{precision}
\title{Precision variance component analysis}
\usage{
precision(data, form, by = NULL, NegVC = FALSE, ...)
}
\arguments{
\item{data}{Data frame containing the response variable and design factor columns.}

\item{form}{Formula parsed by VCA (e.g. \code{y ~ day/run/rep}).}

\item{by}{Column name(s) for sample grouping, supports multiple columns.}

\item{NegVC}{Allow negative variance components? Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{VCA::anovaVCA}.}
}
\value{
Returns an object of class \code{precision}.
Use \code{\link{summary}}, \code{\link{profile}}, etc. to view results.
}
\description{
For each sample (grouped by \code{by}), estimate variance components via
\code{VCA::anovaVCA}, compute SD / CV and their Satterthwaite confidence intervals.
}
\examples{
  data(VCAdata1, package = "VCA")
  precision(VCAdata1, y ~ day/run, by = "sample")

  # More complex: deeply nested factors with more sample groups
  data(realData, package = "VCA")
  precision(realData, y ~ lot/calibration/day/run, by = "PID")
}
