% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/005-fit.R
\name{predict.fit_equation}
\alias{predict.fit_equation}
\title{Predict fitted values or inverse-predict (from y to x)}
\usage{
\method{predict}{fit_equation}(
  object,
  newdata = NULL,
  x = NULL,
  y = NULL,
  inverse = FALSE,
  interval = NULL,
  level = 0.95,
  ...
)
}
\arguments{
\item{object}{a fit_equation object}

\item{newdata}{new data as a data.frame}

\item{x}{x column name; NULL uses the original x column from the fit}

\item{y}{y column name (inverse only); NULL uses the original y column}

\item{inverse}{logical; if TRUE, solve for x given y values in newdata}

\item{interval}{"confidence" or "prediction"}

\item{level}{confidence level (default 0.95)}

\item{...}{additional arguments passed to the underlying predict()}
}
\value{
data.frame with x, y, and (if requested) confidence/prediction interval columns
}
\description{
Predict fitted values or inverse-predict (from y to x)
}
\examples{
df4plc <- data.frame(
  conc = c(0.1, 0.3, 1, 3, 10, 30, 100),
  resp = c(12, 25, 48, 72, 88, 96, 99)
)
f <- fit_equation("E07", df4plc, "conc", "resp")

# Fitted values
predict(f)

# Confidence interval
predict(f, interval = "confidence")

# Prediction interval
predict(f, interval = "prediction")

# Inverse prediction: estimate x for y=50
predict(f, newdata = data.frame(resp = 50), inverse = TRUE)
}
