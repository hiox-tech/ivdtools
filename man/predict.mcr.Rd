% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/007-method-comparison.R
\name{predict.mcr}
\alias{predict.mcr}
\title{Predict (S3 method) -- predict based on regression results}
\usage{
\method{predict}{mcr}(
  object,
  newdata = NULL,
  reference = NULL,
  candidate = NULL,
  inverse = FALSE,
  interval = c("none", "confidence", "prediction", "both"),
  conf.level = 0.95,
  ci_method = c("auto", "analytic", "jackknife", "rank", "bootstrap_percentile",
    "bootstrap_standard"),
  bootstrap_replicates = 5000L,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{mcr object (must call regression() first)}

\item{newdata}{Data frame containing input values. If provided,
reference or candidate names a column in it.}

\item{reference}{Reference-method input for forward prediction. With
newdata, a column name; otherwise a numeric vector.
If NULL, uses the original reference values.}

\item{candidate}{Candidate-method input for inverse prediction. With
newdata, a column name; otherwise a numeric vector.
If NULL, uses the original candidate values.}

\item{inverse}{If FALSE, predict candidate from reference. If TRUE,
algebraically predict reference from candidate.}

\item{interval}{Interval type: "none" (default), "confidence", "prediction", "both".
Only forward prediction (inverse=FALSE) supports intervals.}

\item{conf.level}{Confidence level, default 0.95}

\item{ci_method}{Parameter-uncertainty method. \code{"auto"} reuses the stored
regression method except that Passing-Bablok uses percentile Bootstrap.}

\item{bootstrap_replicates}{Number of paired Bootstrap resamples; at least
5000 when a Bootstrap method is used.}

\item{seed}{Optional integer seed.}

\item{...}{Additional arguments}
}
\value{
data.frame containing predicted values and (if requested) interval bounds
}
\description{
Predict values based on stored regression results.
}
\examples{
  obj <- mcr(ivd_mcr_example, "sid", "test", "ref")
  obj <- regression(obj, method = "ols")
  predict(obj, reference = c(40, 50, 60))
  predict(obj, reference = c(40, 50, 60), interval = "both")
  predict(obj, newdata = data.frame(xx = c(40, 50)), reference = "xx")
  predict(obj, inverse = TRUE)  # Inverse prediction on original data
}
