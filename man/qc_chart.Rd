% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/009-qc.R
\name{qc_chart}
\alias{qc_chart}
\title{Levey-Jennings QC chart + Westgard rule evaluation}
\usage{
qc_chart(
  data,
  value,
  rules = "1-2s,1-3s,2-2s,R-4s,4-1s,10x",
  mean = NULL,
  sd = NULL,
  group = NULL,
  run = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{value}{Column name (numeric) containing measured values.}

\item{rules}{Comma-separated rule names, e.g. "1-2s,1-3s,10x".
Rule names must match those printed by list_westgard().}

\item{mean}{Target mean; NULL to estimate from the selected data column.}

\item{sd}{Target SD; NULL to estimate from the selected data column.}

\item{group}{Optional grouping column name (e.g. QC lot), used for
faceted plots.}

\item{run}{Run-order column name (optional, default 1:n).}
}
\value{
An object of class \code{"qc_chart"} with violation results
and plot data. Use \code{print()} to show summary and \code{plot()}
to draw the Levey-Jennings chart.
}
\description{
Draws a Levey-Jennings QC chart and detects out-of-control points
based on selected Westgard rules. If target mean and SD are not
provided, they are estimated from the data.
}
\examples{
  set.seed(42)
  df <- data.frame(
    run   = 1:30,
    value = c(rnorm(27, 100, 5), 108, 115, 85)
  )
  res <- qc_chart(df, "value", rules = "1-2s,1-3s,10x")
  print(res)
  plot(res)
}
