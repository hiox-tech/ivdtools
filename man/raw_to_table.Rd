% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/010-qualitative-analysis.R
\name{raw_to_table}
\alias{raw_to_table}
\title{Core function: raw data -> frequency fourfold table (silent construction)}
\usage{
raw_to_table(
  data,
  candidate,
  reference,
  id = NULL,
  na.rm = TRUE,
  positive = NULL
)
}
\arguments{
\item{data}{Data frame, one observation per row}

\item{candidate}{Candidate method variable name (character), e.g. "method_new"}

\item{reference}{Reference method variable name (character), e.g. "method_standard"}

\item{id}{ID variable name (character), optional, used for duplicate detection in describe}

\item{na.rm}{Whether to remove NA, default TRUE}

\item{positive}{Specify the positive label (character); if non-NULL, levels will be uniformly mapped to positive/negative.
If NULL, the last sorted level is treated as positive.}
}
\value{
A fourfold_table object (list with S3 class "fourfold_table"), containing:
- $freq_raw        Raw frequencies (long format)
- $table           Fourfold matrix (with margins)
- $n               Total sample size
- $print           Print slot (descriptive metadata)
- $candidate_levels  Levels of the candidate method
- $reference_levels  Levels of the reference method
Use print() directly to display the fourfold table.
}
\description{
Core function: raw data -> frequency fourfold table (silent construction)
}
\examples{
  df <- data.frame(
    new    = c("positive","positive","negative","negative","positive","negative"),
    gold   = c("positive","negative","positive","negative","positive","positive"),
    age    = c(45, 52, 38, 61, 47, 55),
    gender = c("M","F","F","M","M","F"),
    id     = c("S001","S002","S003","S004","S005","S006")
  )
  result <- raw_to_table(df, "new", "gold", id = "id")
}
