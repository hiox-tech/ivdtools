% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/005-fit.R
\name{replicate_to_mean}
\alias{replicate_to_mean}
\title{Summarize replicate measurements}
\usage{
replicate_to_mean(data, x, y, weights = NULL, na.rm = TRUE)
}
\arguments{
\item{data}{a data frame}

\item{x}{x column name (grouping variable)}

\item{y}{One or more y column names (response variables). A single
response retains the historical \code{y_mean}, \code{y_sd}, and \code{y_n} column names;
multiple responses use \verb{<name>_mean}, \verb{<name>_sd}, and \verb{<name>_n}.}

\item{weights}{weighting method: NULL / "n" / "1/sd" / "1/sd^2"
NULL     -- summary only, no weight column
"n"      -- w = number of replicates
"1/sd"   -- w = 1/sd(y)
"1/sd^2" -- w = 1/var(y) (inverse-variance weighting)}

\item{na.rm}{remove NA? (default TRUE)}
}
\value{
A data.frame with columns:
\item{x}{grouping variable}
\item{y_mean, y_sd, y_n}{Historical names used for one response.}
\item{\if{html}{\out{<name>}}_mean, \if{html}{\out{<name>}}_sd, \if{html}{\out{<name>}}_n}{Names used for each response when
two or more response columns are supplied.}
\item{weight}{(only when weights is specified) weight values}
Attributes: call, weights (method name), x_col, y_col
}
\description{
Groups data by x, computes mean, SD, and sample size for each group.
When \code{weights} is specified, returns an additional \code{weight} column.
Summary metadata (call, weighting method, column names) is stored as
attributes for downstream reference.
}
\examples{
# Basic summary
df <- data.frame(
  conc = rep(c(1, 2, 5, 10), each = 3),
  resp = c(3.1, 3.2, 2.9, 5.8, 6.1, 5.9,
           14.2, 14.5, 14.0, 28.1, 27.9, 28.3)
)
rep <- replicate_to_mean(df, "conc", "resp")
rep

# Inverse-variance weighting
rep_w <- replicate_to_mean(df, "conc", "resp", weights = "1/sd^2")
rep_w
}
