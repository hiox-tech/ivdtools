% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/013-sample-size.R
\name{sample_size_proportion}
\alias{sample_size_proportion}
\title{Sample size for a single-proportion target-value test}
\usage{
sample_size_proportion(
  p0,
  p1,
  alpha = 0.05,
  power = 0.8,
  alternative = c("greater", "less", "two.sided"),
  test = c("exact", "score", "score-cc", "wald", "wald-cc", "agresti-coull", "jeffreys"),
  power_method = c("binomial", "normal"),
  n.max = 1e+06
)
}
\arguments{
\item{p0}{Null or target proportion in \verb{(0, 1)}.}

\item{p1}{Anticipated proportion under the alternative in \verb{(0, 1)}.}

\item{alpha}{Target significance level in \verb{(0, 1)}.}

\item{power}{Target power in \verb{(0, 1)}.}

\item{alternative}{One of \code{"greater"}, \code{"less"}, or \code{"two.sided"}.}

\item{test}{Rejection-region method. One of \code{"exact"}, \code{"score"},
\code{"score-cc"}, \code{"wald"}, \code{"wald-cc"}, \code{"agresti-coull"}, or
\code{"jeffreys"}.}

\item{power_method}{Power calculation used to select the sample size:
\code{"binomial"} enumerates the discrete binomial distribution and
requires both actual alpha and actual power to meet their targets;
\code{"normal"} uses the continuous normal approximation and is available
for exact, Score, and Wald tests, with or without continuity correction.}

\item{n.max}{Maximum sample size considered. Default 1000000.}
}
\value{
An object of class \code{sample_size_proportion}. The required
sample size is available as \code{x$n}. The object also contains target
and planning power, exact achieved power, actual alpha, and critical
success counts.
}
\description{
Calculates the minimum sample size required to test a single proportion
against a target value. The test statistic and the power-calculation method
are selected independently. Regardless of the planning method, the returned
result includes the exact binomial Type I error and power of the resulting
discrete rejection region.
}
\details{
\verb{test = "score", power_method = "normal"} uses the null variance for
the alpha term and the alternative variance for the beta term. This is the
normal-approximation procedure used by the CMDE target-value formula.
With an infinite population, \code{test = "exact"} and \code{test = "score"} have
the same normal-approximation formula, matching PASS. The test choice still
determines the discrete rejection region used for the reported actual alpha
and achieved power.
}
\examples{
# Exact binomial design
sample_size_proportion(
  p0 = 0.2, p1 = 0.35, alpha = 0.05, power = 0.8,
  alternative = "greater"
)

# CMDE continuous normal approximation
sample_size_proportion(
  p0 = 0.85, p1 = 0.90, alpha = 0.05, power = 0.8,
  alternative = "two.sided", test = "score", power_method = "normal"
)

}
