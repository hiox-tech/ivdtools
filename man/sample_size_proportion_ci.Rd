% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/013-sample-size.R
\name{sample_size_proportion_ci}
\alias{sample_size_proportion_ci}
\title{Sample size for a single-proportion confidence interval}
\usage{
sample_size_proportion_ci(
  p,
  half_width,
  conf.level = 0.95,
  interval = c("two.sided", "lower", "upper"),
  method = c("wilson", "wald", "wald-cc", "agresti-coull", "jeffreys", "wilson-cc",
    "clopper-pearson"),
  n.max = 1e+06
)
}
\arguments{
\item{p}{Anticipated proportion in \verb{[0, 1]}. Boundary values are useful for
one-sided exact intervals.}

\item{half_width}{For a two-sided interval, one half of the total interval
width. For a one-sided interval, the maximum distance from the anticipated
proportion to the confidence limit.}

\item{conf.level}{Confidence level. Default 0.95.}

\item{interval}{One of \code{"two.sided"}, \code{"lower"}, or \code{"upper"}.}

\item{method}{Confidence interval method. One of \code{"wald"},
\code{"wald-cc"}, \code{"wilson"}, \code{"wilson-cc"}, \code{"agresti-coull"},
\code{"jeffreys"}, or \code{"clopper-pearson"}.}

\item{n.max}{Maximum sample size considered. Default 1000000.}
}
\value{
An object of class \code{sample_size_proportion_ci}. The required
sample size is available as \code{x$n}. The returned object also contains
the achieved half-width and the exact binomial coverage at the assumed
value of \code{p}.
}
\description{
Calculates the minimum sample size whose two-sided confidence interval has
total width no greater than twice \code{half_width}, or whose one-sided limit is
no farther than \code{half_width} from the anticipated sample proportion.
}
\examples{
sample_size_proportion_ci(p = 0.3, half_width = 0.05)
sample_size_proportion_ci(
  p = 0.3, half_width = 0.03, method = "clopper-pearson"
)

}
