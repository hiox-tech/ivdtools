% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/015-sensitivity.R
\name{sensitivity_loq}
\alias{sensitivity_loq}
\title{Establish a limit of quantitation}
\usage{
sensitivity_loq(
  data,
  result,
  sample,
  assigned,
  lot = NULL,
  method = c("westgard", "rms", "precision", "custom"),
  goal,
  goal_scale = c("relative", "absolute"),
  lod = NULL,
  k = 2,
  accuracy_function = NULL,
  profile = FALSE,
  profile_model = c("linear", "quadratic"),
  lot_rule = c("ep17", "separate", "combined"),
  exclude = NULL,
  ...
)
}
\arguments{
\item{data}{Replicate-level data frame.}

\item{result, sample}{Column names for result and sample ID.}

\item{assigned}{Assigned-value column or numeric value/vector.}

\item{lot}{Optional reagent-lot column.}

\item{method}{Accuracy definition.}

\item{goal}{Accuracy goal; proportions are used for relative goals.}

\item{goal_scale}{Relative or absolute scale.}

\item{lod}{Optional numeric LoD or \code{sensitivity_lod} object.}

\item{k}{SD multiplier for the Westgard definition.}

\item{accuracy_function}{Function used by \code{method = "custom"}.}

\item{profile}{Fit an error profile and calculate its goal crossing?}

\item{profile_model}{Linear or quadratic error profile.}

\item{lot_rule}{EP17, separate, or combined lot handling.}

\item{exclude}{Optional excluded rows.}

\item{...}{Additional arguments passed to \code{accuracy_function}.}
}
\value{
An object of class \code{sensitivity_loq}.
}
\description{
Establishes LoQ from Westgard total error, RMS error, a precision goal, or
a user-supplied accuracy function. The original candidate and the LoD-
constrained reported LoQ are retained separately.
}
\examples{
loq_data <- data.frame(
  sample = rep(paste0("L", 1:3), each = 4),
  assigned = rep(c(0.5, 1, 2), each = 4),
  result = c(0.42, 0.48, 0.51, 0.46,
             0.91, 1.02, 0.96, 1.05,
             1.91, 2.04, 1.98, 2.08)
)
loq <- sensitivity_loq(
  loq_data, "result", "sample", assigned = "assigned",
  method = "precision", goal = 0.15, goal_scale = "relative"
)
loq$summary
}
\references{
CLSI. Evaluation of Detection Capability for Clinical
Laboratory Measurement Procedures. EP17-A2, 2nd ed. 2012.
}
