% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/019-dilution.R
\name{spike_recovery}
\alias{spike_recovery}
\title{Calculate spike recovery against a solvent control}
\usage{
spike_recovery(
  data,
  sample,
  result,
  condition,
  analyte_level,
  solvent_level,
  added_concentration = NULL,
  sample_volume = NULL,
  stock_concentration = NULL,
  spike_volume = NULL,
  conf.level = 0.95
)
}
\arguments{
\item{data}{Data frame in long format.}

\item{sample}{Sample-identification column.}

\item{result}{Numeric measurement-result column.}

\item{condition}{Column distinguishing analyte-spiked and solvent-control
preparations.}

\item{analyte_level}{Value identifying the analyte-spiked preparation.}

\item{solvent_level}{Value identifying the solvent-control preparation.}

\item{added_concentration}{A finite scalar or numeric column containing the
theoretical concentration added to the final mixture. When omitted,
\code{sample_volume}, \code{stock_concentration}, and \code{spike_volume} are required.}

\item{sample_volume, stock_concentration, spike_volume}{Finite scalars or
numeric columns used to calculate the added concentration.}

\item{conf.level}{Confidence level for the across-sample mean recovery.}
}
\value{
An object of class \code{spike_recovery}.
}
\description{
Calculates the difference between analyte-spiked and equal-volume
solvent-control means and expresses that difference as a percentage of the
known added concentration. No acceptance criterion is applied.
}
\examples{
spike_data <- data.frame(
  sample = rep(c("S1", "S2"), each = 4),
  condition = rep(rep(c("spiked", "control"), each = 2), 2),
  result = c(20.1, 19.9, 10.0, 10.2, 30.2, 29.8, 20.1, 19.9)
)
recovery <- spike_recovery(
  spike_data, "sample", "result", "condition",
  analyte_level = "spiked", solvent_level = "control",
  added_concentration = 10
)
recovery$results
}
\references{
CLSI. Establishing and Verifying an Extended Measuring Interval
Through Specimen Dilution and Spiking. EP34, 1st ed. 2018.
}
