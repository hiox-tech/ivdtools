% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/007-method-comparison.R
\name{split_by_cutpoints}
\alias{split_by_cutpoints}
\title{Split a data frame at fixed numeric cutpoints}
\usage{
split_by_cutpoints(
  data,
  variable,
  cutpoints,
  labels = NULL,
  na_action = c("error", "drop"),
  drop_empty = FALSE
)
}
\arguments{
\item{data}{A data frame.}

\item{variable}{A single character string naming the numeric variable used
to split the rows.}

\item{cutpoints}{A non-empty, finite, strictly increasing numeric vector.}

\item{labels}{Optional unique names for the returned data frames. Its length
must be \code{length(cutpoints) + 1}. By default the names are \code{segment_1},
\code{segment_2}, and so on.}

\item{na_action}{How non-finite splitting values are handled: \code{"error"}
(default) stops; \code{"drop"} excludes them and records the excluded count.}

\item{drop_empty}{Whether empty intervals are removed. The default is
\code{FALSE}, so the returned list preserves all prespecified intervals.}
}
\value{
A named list of data frames with class \code{cutpoint_split}. The
\code{segment_info} attribute records interval bounds, labels, and row counts.
}
\description{
Split an input data frame into a named list of data frames before analysis.
Cutpoints are applied to one numeric variable using left-closed,
right-open intervals. For example, cutpoints \code{c(100, 500)} create
\verb{[-Inf, 100)}, \verb{[100, 500)}, and \verb{[500, Inf)}.
}
\examples{
parts <- split_by_cutpoints(
  ivd_mcr_example, variable = "ref", cutpoints = c(18, 26)
)
parts
parts$segment_1
fits <- lapply(parts, function(d) mcr(d, "sid", "test", "ref"))
}
