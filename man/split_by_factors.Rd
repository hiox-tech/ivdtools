% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/007-method-comparison.R
\name{split_by_factors}
\alias{split_by_factors}
\title{Split a data frame by the levels of one categorical variable}
\usage{
split_by_factors(
  data,
  variable,
  na_action = c("drop", "error"),
  drop_empty = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{variable}{A single character string naming the factor, character, or
logical variable used to split the rows.}

\item{na_action}{How missing splitting values are handled: \code{"drop"}
(default) excludes them and records the excluded count; \code{"error"} stops.}

\item{drop_empty}{Whether unused factor levels are removed. The default is
\code{TRUE}. This argument has no effect for character or logical variables,
whose groups are determined from observed non-missing values.}
}
\value{
A named list of data frames with class \code{factor_split}. List names
are the original factor levels or categorical values. The \code{group_info}
attribute records their order and row counts.
}
\description{
Split an input data frame into a named list of data frames before analysis.
Factor levels retain their declared order; character and logical values use
first-appearance order. The splitting column is retained in every returned
data frame.
}
\examples{
d <- data.frame(
  batch = factor(c("Batch-A", "Batch-A", "Batch-B")),
  result = c(10.1, 9.9, 10.4)
)
parts <- split_by_factors(d, variable = "batch")
parts
parts[["Batch-A"]]
means <- lapply(parts, function(z) mean(z$result))
}
