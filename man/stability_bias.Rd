% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/014-stability.R
\name{stability_bias}
\alias{stability_bias}
\title{stability_bias --- Node-wise bias analysis across storage conditions}
\usage{
stability_bias(
  data,
  condition,
  time,
  value,
  reference_condition = NULL,
  limit = NULL,
  bias_type = c("relative", "absolute"),
  time_unit = "d"
)
}
\arguments{
\item{data}{A data frame.}

\item{condition}{Column name for storage condition (e.g. temperature).}

\item{time}{Column name for time point.}

\item{value}{Column name for measurement value.}

\item{reference_condition}{Reference condition for between-condition comparison;
NULL uses the first occurring condition.}

\item{limit}{A positive number for symmetric allowable bias; NULL to skip.}

\item{bias_type}{\code{"relative"} (percent) or \code{"absolute"}.}

\item{time_unit}{Time unit: m, min, h, d, month, or y.}
}
\value{
An S3 object of class \code{"stability_bias"}.
}
\description{
Each storage condition is baseline-corrected to its own first
time point. Absolute and relative biases are computed and compared
across conditions at matched time points.
}
\examples{
set.seed(42)
df <- data.frame(cond = rep(c("2-8C","25C"), each = 8),
                 t = rep(c(0,1,3,7), 4),
                 val = c(rnorm(4,100,2), rnorm(4,98,2),
                         rnorm(4,100,2), rnorm(4,92,3)))
stability_bias(df, "cond", "t", "val", limit = 5)
}
