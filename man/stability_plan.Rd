% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/014-stability.R
\name{stability_plan}
\alias{stability_plan}
\title{stability_plan --- CLSI EP25 Appendix A time-point planning}
\usage{
stability_plan(
  allowable_drift,
  variability,
  replicates = 1L,
  expected_drift = NULL,
  power = c(0.8, 0.9),
  mode = c("simple", "components"),
  bias_type = c("relative", "absolute")
)
}
\arguments{
\item{allowable_drift}{Allowable drift (positive number).}

\item{variability}{Repeatability CV/SD (simple mode) or a data frame of
variance components.}

\item{replicates}{Number of replicate measurements per time point.}

\item{expected_drift}{Expected drift; NULL defaults to half of allowable_drift.}

\item{power}{Statistical power: 0.8 or 0.9.}

\item{mode}{\code{"simple"} or \code{"components"}.}

\item{bias_type}{\code{"relative"} or \code{"absolute"}.}
}
\value{
An S3 object of class \code{"stability_plan"}.
}
\description{
Based on the ratio of expected drift to allowable drift and
the reproducibility variability, looks up the minimum number of time
points required per regression series in the EP25 Appendix A tables.
}
\examples{
stability_plan(4, 1.5, replicates = c(1,2,3))

comp <- data.frame(source = c("repeatability","lot","operator"),
                   variability = c(1.5,1.0,0.8),
                   levels = c(1,3,2))
stability_plan(4, comp, replicates = c(1,2,3), mode = "components")
}
