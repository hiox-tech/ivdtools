% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/020-uncertainty.R
\name{uncertainty_propagate}
\alias{uncertainty_propagate}
\title{Propagate uncertainty through a general measurement model}
\usage{
uncertainty_propagate(
  model,
  components,
  method = c("delta", "monte_carlo"),
  correlation = NULL,
  simulations = 100000L,
  coverage = 0.95,
  seed = NULL
)
}
\arguments{
\item{model}{Function whose named arguments match component names.}

\item{components}{Components or analysis objects containing components.}

\item{method}{\code{"delta"} or \code{"monte_carlo"}.}

\item{correlation}{Optional correlation matrix. Nonidentity correlation in
Monte Carlo mode currently requires normal or standardized components.}

\item{simulations}{Number of Monte Carlo draws.}

\item{coverage}{Coverage probability.}

\item{seed}{Optional random seed.}
}
\value{
An \code{uncertainty_propagation} object.
}
\description{
The delta method numerically estimates sensitivity coefficients and applies
GUM first-order propagation. Monte Carlo propagation samples the stored
component distributions and returns a central quantile interval.
}
\examples{
numerator <- uncertainty_type_b(
  "numerator", estimate = 20, uncertainty = 0.4,
  distribution = "standard"
)
denominator <- uncertainty_type_b(
  "denominator", estimate = 10, uncertainty = 0.1,
  distribution = "standard"
)
propagated <- uncertainty_propagate(
  function(numerator, denominator) numerator / denominator,
  list(numerator, denominator), method = "delta"
)
propagated$estimate
}
\references{
CLSI EP29-A.
}
